<BaseScreen>
    canvas.before:
        Color:
            rgba: app.theme.background
        Rectangle:
            pos: self.pos
            size: self.size
            source: app.assets("bg_gradient_dark.jpg") if app.theme.mode == "dark" else app.assets("bg_gradient_light.jpg")